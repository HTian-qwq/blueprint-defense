# 历史工具归档

这里保留分离前用于素材提取、音效转换、审计与旧版本试玩的脚本原文。部分文件包含原游戏、工具包、下载目录及旧项目的绝对路径，另需 Unity / FFmpeg / .NET 等提取环境。

它们不参与本项目的构建或当前回归。不要将整个目录作为批量命令运行。后续需要重新提取资源时，应先为对应脚本整理输入参数与输出目录；完整原提取环境仍在 `F:\4\endfield_blueprint_td` 与相邻工具目录。

日常开发请使用上级目录的 `build.py`、`serve.py`、`test_current.py`。
