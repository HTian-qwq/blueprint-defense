# 独立项目交接

## v0.18 网页版

已完成双构建输出、静态发布包、加载与重试界面、主菜单、设置、本地自动存档、存档导入／导出、蓝图文件分享与整组付费放置。当前入口和操作见根目录 README。

网页入口和素材分离，文件名带内容哈希。音效和角色动画延迟加载，字体不阻塞网页开始游玩；主菜单打开时暂停高频画布重绘。离线单文件改在 `dist/offline/`，原有逻辑／浏览器回归使用该路径。网页版回归使用临时 HTTP 服务，不依赖用户的 8770 服务。

存档恢复保留攻击对象引用，包括已经撤回但仍有炮弹在途的炮塔；恢复后不重播旧音频事件。全线防守和罗丹挑战隔离存储。蓝图通过临时游戏状态预检全部建造／改装，确认时重新预检，避免部分扣款和部分建造。

暂未部署到公共网址。`dist/blueprint-defense-web.zip` 是可发布产物，包含当前版本引用的文件；不要直接上传包含离线单文件和旧缓存文件的整个 dist 目录。

## 基线与迁移边界

2026-09-21 从 `F:\4\endfield_blueprint_td` 分离到 `F:\4\blueprint-defense`，以 v0.17 为功能基线。原目录及其 8768 服务保留。独立项目使用 8770 端口。

本轮仅处理项目组织、构建输入与开发环境可迁移性，不修改玩法、地图、费用、科技或批量操作规则。

- 游戏源码、全部处理后的素材、测试、原版战斗／敌人／配方表与必要的素材证据已带入。
- 外部工具包的 7 张构建用表固定在 `sources/game_data/tables/`。构建与当前回归不再读取相邻目录。
- 9 条用户语音的原始备份保留在 `sources/audio/wisadel_original/`，测试继续校验转换前后的 SHA-256。
- 逐帧解包缓存、大量中间音频和原始资源包仍保存在旧工程，日常构建无需这些文件。
- 历史提取脚本放在 `tools/archive/`，保留原始内容与路径作为参考。它们仍依赖原提取环境，不是当前构建流程的一部分。
- `docs/migration_manifest.json` 记录每份复制文件的原位置及复制时哈希。此后为去除绝对路径而修改的脚本可能与该快照哈希不同；提交内容由 Git 追踪。

## 当前交互与边界

地图 64×48，底部固定仓库；建筑采用原版占地。1–9 快捷栏可设置机械和配方。X 切批量模式，左键框选追加，右键框选移除，中键平移；长按左键或 M 移动整组，F 拆除。五台及以上先显示游戏内确认；固定核心和仓库不参与选择。

15 项局内研究、22 条配方；蓝铁由研究解锁。界面折金票为内部 `dp` 的 100 倍，当前兑换收益已减半。高级塔需要材料和弹药。不要在优化时无意改变费用单位、接口旋转、研究支付或整组移动的原子性。

尚未实现批量复制、蓝图存储／导入、供电、液体网络和弹药逐塔运输。

## 验证范围

独立目录迁移验证已通过：156 项检查与 12 种传送带朝向。构建出的 HTML 与分离前文件的 SHA-256 完全一致，8770 服务返回的内容也与新构建一致。详见 `docs/baseline/migration_verification.json` 与 `standalone_verification.json`。

当前入口为 `python tools/test_current.py`，覆盖建造、整段铺带、原版批量操作、快捷栏、当前经济／生产、研究解锁、原版占地、攻击／音效／朝向、波间整备与浏览器交互。

历史战斗用例使用 `tests/fixtures/` 内固定数值。当前经济完整性由 economy / industrial 系列验证。`docs/baseline/` 内的真人式完整试玩报告对应更早构建；不能据此声称 v0.17 已完成一次当前版本的完整人工通关。

## 下一轮优化建议

先采集当前构建的性能和操作基线，再分项改动：

1. **加载与资源**：当前离线 HTML 约 43 MiB，完整字体约 20 MiB。先测启动时间和内存，再比较保留离线包与开发版拆资源的收益。
2. **界面更新**：检查 `src/app.js` 的定时界面刷新、重复 DOM 更新和画布绘制，优先减少未变化内容的重建。
3. **大规模生产与战斗**：测多设备、多弹幕时的帧耗时，重点看接口查找、碰撞、敌人检索和特效生命周期。
4. **操作体验**：在现有放置、批量与快捷栏交互上验证误操作、取消、冲突提示和缩放下的命中范围。

这些是待测方向，不是已确认的瓶颈。每项优化独立提交；涉及数值或玩法的调整另行确认范围。优先保留可离线使用和现有素材视觉效果。

## v0.18.1：核心物流与逐格放置

`initCorePorts` 从构建时提取的 `map.core.portCells` 派生固定逻辑接口，复用取货口/存货口输送规则；不加入玩家建筑列表。`productionNodes()` 包含机械与核心接口，底部管线仍按原方式供货。`session.js` 可选保存核心动态数据，几何坐标从配置重建；旧版无此字段时默认关闭。构建的 rulesId 排除新增 portCells，已核对保持 v0.18 的规则指纹。

`src/placement.js` 自动对齐仅原地旋转，移除了相邻一格吸附。点击成功后清除旧位置的悬浮预览。新增 `core_logistics`、`core_logistics_browser` 覆盖全部端口、实际浏览器生产回路、存档及紧贴放置。

验证：全套 185 项检查 + 12 种传送带方向全部通过；8770 网页额外通过 4 项实际浏览器核心物流/放置检查。摘要见 `docs/verification/v0.18.1.json`。

## v0.18.2：原版战斗 BGM

新增 `src/music.js` 和 `src/music_ui.js`，以单个 HTMLAudioElement 按需播放长音乐，不放入短音效解码缓存。默认音量 25%，每两波轮换，罗丹在场时使用第二首；整备减半，暂停/后台/菜单/结算暂停，倍速不改变播放速度。菜单设置提供独立音量、开关、选曲与加载重试，偏好使用 `blueprint-defense-music` 本地键保存。导入存档和重开战役会重置音乐位置。

素材从本机游戏的 `au_music_states_combat` 状态事件追至音乐切换容器、播放列表、片段、轨道和流式媒体；选用完整的单轨战斗循环 162823481（132 秒）和 935948798（94.286 秒）。两首的 WEM 原件、SHA-256、相关 HIRC 字节和来源记录保留在 `sources/music/` 与 `sources/music_provenance.json`。显示名称不是官方曲名。网页两首 MP3 合计约 3.45 MiB，按曲目加载；离线版内嵌。`tools/prepare_music.py` 可重新编码，普通构建不依赖游戏安装或解包工具。

音乐仅在生成页面配置时添加，不参与 balance_reference / rulesId，继续兼容已有存档。`music` 单元检查和 `music_browser` 实际 HTTP 音频检查纳入 `tools/test_current.py`，覆盖循环末端、暂停续播、独立开关、倍速、后台、偏好、手机布局和断网重试。

验证：197 项检查和 12 种传送带朝向通过，含离线版内嵌音乐实际播放。初次 geometry_browser 加载与重新构建重叠而失败，最终构建后单独重跑 7 项全部通过；重跑记录保留在验证摘要 `docs/verification/v0.18.2.json`。

## v0.18.3：字体分片与独立发布验证

原始 HarmonyOS Sans SC TTF 继续作为字体来源保留，网页和离线版均改用 `assets/fonts/subsets/` 中的 58 个 WOFF2 分片。Latin 与游戏源码/配置文字单独分组，其余字符每 512 个一片；`unicode-range` 不重叠，保留全部 29,508 字符和 40–900 可变轴。启动两片共 361,744 字节，原版为 20,617,156 字节。`tools/prepare_fonts.py` 是可选生成流程，标准构建通过 `tools/font_assets.py` 使用已提交分片和 SHA-256 清单，不新增日常构建依赖。

`document.fonts.load` 显式传入游戏字符集，解决画布中文字的字体就绪问题。网页仍不阻塞启动；下载失败保持系统字体。顶部菜单/蓝图按钮从 build.py 注入移到 HTML 模板，保证其文案也被常用字收集。音效、战斗、经济与 rulesId 保持不变。

`tools/test_web_release.py` 直接从发布 ZIP 启动独立静态服务器，在 `/game/index.html` 验证 10 项网页流程、7 项音乐检查、5 项字体检查，全部通过。原字体与分片在浏览器中对常规/粗体样本像素一致；首屏只下载两片，显示额外生僻字只加一片。详见 `docs/DEPLOYMENT.md` 与 `docs/verification/v0.18.3.json`。
